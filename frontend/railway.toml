[build]
builder = "NIXPACKS"
# Railway runs the build from the /frontend Root Directory.
buildCommand = "npm ci && npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/healthz"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
