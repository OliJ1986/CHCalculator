[build]
builder = "NIXPACKS"
# Railway runs the build from the /frontend Root Directory.
# Nixpacks installs lockfile dependencies before the build phase.
buildCommand = "npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/healthz"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
