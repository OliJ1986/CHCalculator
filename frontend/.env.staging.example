# Set these as Railway service variables; do not commit a populated copy.
APP_ENV=staging
BACKEND_URL=http://${{backend.RAILWAY_PRIVATE_DOMAIN}}:${{backend.PORT}}
BACKEND_PROXY_TOKEN=${{backend.STAGING_PROXY_TOKEN}}
STAGING_BASIC_AUTH_USER=SET_IN_RAILWAY
STAGING_BASIC_AUTH_PASSWORD=SET_IN_RAILWAY
VITE_API_BASE_URL=/api
